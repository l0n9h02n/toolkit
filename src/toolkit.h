#include <vector>

using namespace std;

class toolkit
{


    public:
//        toolkit();
//        ~toolkit();
        string stringMultiplication(string, int);
        string stringDivision(string, int);
        vector<string> explode(char, string);

};
